var inicioVendas = document.getElementById('iniciovendas'),
    consultarVendas = document.getElementById('consultarvendas'),
    telaitens = document.getElementById('itens');

if (inicioVendas) {
    var btn = document.getElementById('btniniciovendas');
    btn.classList.add('btnformout-active');
};

if (consultarVendas) {
    var btn = document.getElementById('btnconsultarvendas');
    btn.classList.add('btnformout-active');
};

if (telaitens) {
    var btn = document.getElementById('btnitens');
    btn.classList.add('btnformout-active');
};

